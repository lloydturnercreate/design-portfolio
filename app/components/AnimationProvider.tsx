'use client';

import { ReactNode } from 'react';

/**
 * AnimationProvider
 * Placeholder provider for future animation setup
 * Currently passes through children unchanged
 */
export default function AnimationProvider({ children }: { children: ReactNode }) {
  return <>{children}</>;
}

